@model IEnumerable<Konveter_valyut.Models.Konveter>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Konver_date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Oldingi_valyut)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Keyingi_valyut)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Oldingi_summa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Keyingi_summa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Konver_date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Oldingi_valyut)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Keyingi_valyut)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Oldingi_summa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Keyingi_summa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
